# Variables de entorno exclusivas para `pnpm run test`. Base de datos
# separada de prisma/dev.db para que las pruebas nunca toquen los datos
# de desarrollo.
DATABASE_URL="file:./prisma/test.db"
MAX_PDF_SIZE_MB=5
SESSION_SECRET="test-secret-solo-para-pruebas-automatizadas-1234567890"
